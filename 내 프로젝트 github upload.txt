내 프로젝트 github upload

1. github repository 생성
     https://github.com/junseojeong접속
     New repository
2. local 폴더 repository 선언
= local repository 만들기
     local 폴더에서 git bash here

     @ git init

3. config 설정
= local repository와
    remote repository의 권한,
    보안 설정

    @ git config --local user.name junseojeong
    @ git config --local user.email jsjeong96@gmail.com



4. remote 설정

     upload할 remote
     repository주소 별명 설정
     
5. 파일 add
6. commit 설정
7. push